const
  { test, fetch, read }
    = require ('snuggsi')


test `Payload from test json`

( async assert => {

  let
    payload =
      await fetch `https://jsonplaceholder.typicode.com/todos/1`

  , json =
      await payload.json ``

  , text =
      await read `index.go`


  console.log ('Payload', json)

  assert ('title' in json)
})
