const
  { test, fetch, read }
    = require ('snuggsi')



(async assert => {

  let response = await fetch
    `https://jsonplaceholder.typicode.com/todos/1`
  
  assert
    ( await read `index.go` )
    ( 'title' in await response.json `` )
})
